services:
  wtchk-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: wtchk_backend
    restart: unless-stopped
    environment:
      - FASTAPI_ROOT_PATH=/wtchk/api
      - DATABASE_NAME=wtchk
    env_file:
      - .env
    ports:
      - "8000:8000"
    networks:
      - connex_network
    volumes:
      - wtchk_upload_tasks:/app/upload_tasks

volumes:
  wtchk_upload_tasks:
    driver: local

networks:
  connex_network:
    external: true