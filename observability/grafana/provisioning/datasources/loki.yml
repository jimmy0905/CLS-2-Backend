apiVersion: 1
prune: true

datasources:
  - name: CLSense Loki
    uid: clsense-loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: false
    jsonData:
      timeout: 60
      maxLines: 1000
